# H11 真机整链路复测记录（2026-04-07）

## 设备与环境

- 设备：Pixel 3（Android 12）
- 包名：`com.example.ocrjizhang`
- 复测目标：按 H11 顺序复测登录、分类、交易、OCR、资产、统计、同步

## 1. 登录与会话

### 1.1 启动自动进入主页

- 步骤：冷启动 App
- 预期：若本地会话有效，直接进入登录后主页面
- 实际：通过，直接进入“账本”页
- 备注：说明本地会话保持正常

### 1.2 退出登录

- 步骤：进入“我的”页，点击“退出登录”
- 预期：清理本地会话并回到登录页
- 实际：通过，返回登录页，并显示“已退出登录”

### 1.3 演示账号重新登录

- 步骤：点击“本机演示账号”卡片中的“一键体验演示账号”
- 预期：使用 `demo / 123456` 登录并回到账本首页
- 实际：通过，返回“账本”页，并显示“登录成功”
- 备注：中途一度误判为跳转到 OCR 页，已确认是自动化脚本在登录成功后又额外点击了一次原坐标，实际登录路由无异常

## 2. 分类管理

### 2.1 从“我的”进入分类管理

- 步骤：进入“我的”页，点击“分类管理”
- 预期：进入分类管理页，默认显示支出分类
- 实际：通过，页面标题、切换按钮、默认分类列表和说明文案正常显示

### 2.2 默认分类首屏检查

- 步骤：检查分类列表首屏与下滑列表
- 预期：默认支出分类可见，包含默认标记
- 实际：通过，已确认首屏/下滑后均有默认分类，如“买菜 / 交通 / 住房 / 医疗 / 娱乐 / 学习 / 宠物 / 打车”等

### 2.3 自定义分类新增

- 步骤：打开“新增支出分类”弹窗，输入临时名称并尝试创建
- 预期：创建成功后出现在分类列表，可继续编辑/删除
- 实际：本轮自动化结果未确认通过
- 备注：
  - 真机上输入法弹出时，底部“创建”按钮点击容易被输入法区域干扰
  - 本轮通过 adb 自动点击未稳定复现“创建成功”，数据库查询中也未看到新增记录
  - 当前结论更像是自动化点击精度问题，尚未证明业务逻辑存在缺陷；后续建议补一次人工真机点击验证

## 3. 当前结论

- 登录与会话主路径：通过
- 分类入口与默认分类展示：通过
- 自定义分类新增：待人工补一次真机点击确认

## 3. 手动交易闭环

### 3.1 新增一笔手动支出

- 步骤：从“账本”页右下角 FAB 打开新增记账弹层，切换资金账户为“支付宝”，选择默认分类“娱乐”，录入金额 `12.60` 并保存
- 预期：返回账本后，本月支出、最近记账、资产页账户余额同步更新
- 实际：通过
- 备注：
  - 账本页月支出从 `￥20.80` 变为 `￥33.40`
  - 资产页总资产从 `￥79.20` 变为 `￥66.60`
  - 资产页“支付宝”余额从 `￥0.00` 变为 `-￥12.60`

### 3.2 编辑刚新增的交易

- 步骤：在“账本”页下滑到“最近记账”，点击刚新增的“娱乐”记录，进入编辑弹层后将金额从 `12.60` 改为 `8.80`
- 预期：更新成功，账本与资产同步回写
- 实际：通过
- 备注：
  - 编辑弹层已正确回填分类、金额、账户和时间
  - 资产页总资产从 `￥66.60` 回升到 `￥70.40`
  - “支付宝”余额同步变为 `-￥8.80`

### 3.3 删除刚编辑的交易

- 步骤：再次从“最近记账”打开该记录，点击弹层顶部“删除”，在确认弹窗中确认删除
- 预期：记录从本地移除，账本与资产恢复到删除前状态
- 实际：通过
- 备注：
  - 删除确认弹窗文案和按钮正常显示
  - 删除后资产页总资产恢复为 `￥79.20`
  - “支付宝”余额恢复为 `￥0.00`

## 4. OCR 带入记账

### 4.1 选择图片并完成本地识别

- 步骤：进入 OCR 页，使用系统 Photo Picker 选择一张本地图像，然后点击“开始识别”
- 预期：识别结果卡显示金额、日期、商户和原始识别文本
- 实际：通过
- 备注：
  - 本轮识别结果为：金额 `14.4`，日期 `未识别`，商户 `DSPIAEE斯派`
  - 原始识别文本中可见 `DSPIAE迪斯派碳纤维打磨¥14.4`
  - 自动化点击“开始识别”时有过一次未在原位置立即观察到结果卡的情况；复测后确认结果卡实际已生成，更多是滚动位置导致的观察偏差，不是本轮阻塞项

### 4.2 带入新增记账并保存

- 步骤：在 OCR 结果卡点击“带入记账表单”，确认金额、商户、时间已预填，直接保存
- 预期：OCR 结果进入新增记账弹层并成功生成一笔交易
- 实际：通过
- 备注：
  - 新增记账弹层显示 OCR 预填提示卡
  - 金额预填为 `¥14.4`
  - 详情按钮文案预填为 `DSPIAEE斯派`
  - 默认资金账户为“支付宝”
  - 保存后资产页总资产变为 `￥64.80`，“支付宝”余额变为 `-￥14.40`

## 5. 统计页

### 5.1 月度统计展示

- 步骤：进入“统计”页，保持“月”维度，检查当前月份数据
- 预期：收支概览、均值与图表区域展示真实数据
- 实际：通过
- 备注：
  - 当前范围正确显示为 `2026年04月`
  - 页面显示：支出 `￥35.20`、收入 `￥0.00`、结余 `-￥35.20`、日均支出 `￥1.17`
  - 柱状图区域已正常渲染

## 6. 本地同步

### 6.1 同步入口与本机后端联通

- 步骤：确认 `local.properties` 中 `base.url=http://192.168.50.197:8080/api/`，启动本机 `backend` 模块，再在“我的”页点击“立即同步”
- 预期：本地账户 / 分类 / 交易推送到演示后端，并可从后端拉取汇总结果
- 实际：通过
- 备注：
  - 复测开始时 `8080` 未运行，属于环境问题，不是客户端逻辑缺陷
  - 启动本机后端后，`/api/auth/login` 与业务接口可正常访问
  - 通过本机后端接口复核，当前云端数据为：`5` 个账户、`34` 个分类、`2` 条交易
  - 说明真机点击同步后，演示后端已经收到本地账本数据

## 7. 最终退出登录

### 7.1 结束态退出

- 步骤：完成统计与同步验证后，再次进入“我的”页并点击“退出登录”
- 预期：清理会话并返回登录页
- 实际：通过，已返回登录页
- 备注：登录页中的“本机演示账号”卡片与用户名 / 密码输入区显示正常

## 4. 下一步

- H11 主链路已完成，当前仅剩“分类新增”建议补一次人工真机点击确认
- 若继续下一轮，可优先补：
  - 分类新增的人手点击确认与回写结果
  - OCR 页“选择图片后立即点击开始识别”的自动化点击稳定性观察
  - 同步完成后的 Snackbar 文案抓取与补录
